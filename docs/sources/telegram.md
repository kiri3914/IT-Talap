# Источник: Telegram-каналы с IT-вакансиями (Казахстан и Центральная Азия)

**Исследовал:** Claude (агент-разведчик)
**Дата проверки:** 2026-09-04
**URL:** https://t.me/s/<channel> — веб-превью публичных каналов

> Всё, что ниже, проверено через веб-превью `t.me/s/<name>` без авторизации.
> Через Telethon/Bot API ничего не проверялось — там, где так, стоит «не проверено».

---

## 1. Вердикт

**Рекомендация:** брать в Фазу 2 (после hh), и только как дополняющий источник

**Обоснование в одну строку:** каналы живые, зарплата указана заметно чаще, чем на джоб-бордах, и часть вакансий нигде больше не публикуется — но у постов нет ни стабильного ID вакансии, ни схемы, поэтому парсинг и дедупликация стоят дорого и всегда будут вероятностными.

---

## 2. Объём и покрытие

| Параметр | Значение | Как проверил |
|---|---|---|
| Всего IT-вакансий сейчас | «Ленты», а не каталог: снапшота нет. Глубина архива в превью — тысячи постов (у @findwork счётчик ссылок ~9.55K) | t.me/s/, 2026-09-04 |
| По Казахстану | 3 профильных IT-канала: @workitkz, @devkz_jobs (+ @jobkz_1 как общий, IT почти нет) | превью |
| По Узбекистану | 2 профильных: @uzdev_jobs, @itmarket_uz (+ @headhunter_uz — общий, ретрансляция hh.uz) | превью |
| По Кыргызстану | 1 профильный: @findwork (Jobs \| DevKG) (+ @vacancykgg — общий, IT почти нет) | превью |
| Новых в день (оценка) | @workitkz, @devkz_jobs, @uzdev_jobs, @findwork — каждый десятки постов/сутки: у @devkz_jobs и @findwork все ~20 последних постов уложились в один рабочий день | по таймстемпам последних постов |
| Доля с указанной зарплатой | 85–100 % в профильных IT-каналах (см. таблицу ниже) — существенно выше, чем на hh | глазами по ~20–30 последним постам каждого канала |

Как считал долю с зарплатой: визуально по 20–30 последним постам в веб-превью каждого канала на 2026-09-04. Это оценка, не подсчёт по выборке; статистически корректную долю надо мерить уже после сбора.

---

## 2a. Каналы (главная таблица)

| Канал | Ссылка | Подписчиков | Активность | Формат постов | Зарплата | Ссылки на hh | География |
|---|---|---|---|---|---|---|---|
| IT Вакансии Казахстан | https://t.me/s/workitkz | 35.2K | ежедневно, много постов | шаблон: Должность / Компания / Город / Занятость / Оплата / описание / контакт | ~100 % (заявленная политика канала: «только вакансии с указанием зп») | не замечено | Астана, Алматы, + Караганда, Актобе, Жезказган; есть удалёнка |
| Dev KZ \| Vacancy | https://t.me/s/devkz_jobs | 24.8K | ~20 последних постов за один день | полушаблон: Компания / Обязанности / Требования / Условия / З/п / контакт | ~85–90 % | не замечено | Астана, Алматы, удалёнка |
| JobKZ: вакансии / работа в Казахстане | https://t.me/s/jobkz_1 | 36.6K | ~20 постов за пару часов | шаблон (должность/локация/формат/вилка/задачи/требования) | высокая, но вилки часто «до X ₸» с KPI | не замечено | Алматы, Астана, Шымкент; редко РФ |
| UzDev Jobs — IT Jobs | https://t.me/s/uzdev_jobs | 25.9K | ежедневно | полушаблон: должность+хэштеги / компания / обязанности / требования / условия / контакт | ~85 % (UZS и USD) | не замечено | Ташкент (подавляюще), Наманган; офис/гибрид/удалёнка |
| IT MARKET | https://t.me/s/itmarket_uz | 12 225 | не проверено | не проверено | не проверено | не проверено | заявлено «IT-вакансии и резюме со всего СНГ» |
| hh.uz — работа в Узбекистане | https://t.me/s/headhunter_uz | 22.5K | несколько постов в день | шаблон карточки hh: компания / город / должность-ссылка / вилка / gross-net | указана часто, но регулярно «Уровень дохода не указан» | да — это ретранслятор hh.uz | Ташкент, Самарканд, Фергана, Андижан, Ахангаран, встречается Алматы |
| Jobs \| DevKG | https://t.me/s/findwork | 22.3K | ~20 постов за одно утро | самый жёсткий шаблон: компания / должность / формат / вилка / хэштеги / ссылка на devkg.com/tg/j-<ID> | ~100 % (KGS или USD) | нет; вместо этого ссылка на devkg.com | Бишкек (все посты с #bishkek) |
| ВАКАНСИИ БИШКЕК | https://t.me/s/vacancykgg | 2.64K | ежедневно | свободный текст, много повторов одних и тех же объявлений | часто | не замечено | Бишкек; часть вакансий — в РФ |
| СЕТИ — IT & Digital вакансии | https://t.me/s/it_vakansii_jobs | 94.5K | ежедневно, дайджестами | подборки + отдельные карточки | часто (₽ и $) | не замечено | преимущественно РФ/удалёнка; Алматы встречается изредка |

Итого проверено 9 каналов. Профильно-релевантных для нашего региона — 5: **@workitkz, @devkz_jobs, @uzdev_jobs, @findwork, @headhunter_uz**. @jobkz_1 и @vacancykgg — общие джоб-каналы, IT в них единицы; @it_vakansii_jobs — по сути российский; @itmarket_uz не отдал ленту в превью.

---

## 3. Способ доступа

**Тип:** Telegram (веб-превью + MTProto-клиент)

| Вопрос | Ответ |
|---|---|
| Есть публичный API? | Да, MTProto (Telethon/TDLib). Bot API для чтения чужих каналов не годится — бот должен быть админом канала. |
| Нужна регистрация приложения / токен? | Да: `api_id` + `api_hash` на https://my.telegram.org + номер телефона для авторизации user-сессии. Аккаунт-«читатель» = живой номер. |
| Документация API (URL) | https://core.telegram.org/api, https://docs.telethon.dev |
| Работает ли без авторизации | Частично: `https://t.me/s/<channel>` отдаёт HTML-ленту публичного канала без логина — этим и собраны данные выше. Пагинация назад через `?before=<msg_id>`. Для полной истории и надёжности нужен MTProto. |
| Есть ли пагинация и её лимиты | HTML: `?before=<id>`, ~20 постов на страницу, глубина ограничений в лоб не тестировалась. MTProto `GetHistoryRequest`: до 100 сообщений за запрос, вся история доступна. |
| Есть ли полное описание вакансии в списке | Да — пост и есть вакансия целиком, отдельный запрос не нужен. Длинные посты в HTML-превью могут быть обрезаны — не проверено. |
| Есть ли фильтр по дате публикации | В MTProto — `offset_date`. В HTML — только перемотка по `before`. |
| Есть ли фильтр по профессии/категории | Нет. Только хэштеги, и только там, где канал их ставит (@findwork, @uzdev_jobs). |

**Пример запроса, который реально отработал:**

```
GET https://t.me/s/workitkz
GET https://t.me/s/devkz_jobs
GET https://t.me/s/findwork
```
(HTML-лента, без авторизации, 2026-09-04)

**Пример ответа (обрезанный):**

```
Полный HTML со списком постов; в каждом <div class="tgme_widget_message"
data-post="workitkz/<msg_id>"> — текст поста, время, реакции.
Сырой JSON здесь неприменим: превью отдаёт HTML, а не API-ответ.
Пример ответа MTProto не приводится — Telethon в этой разведке не запускался.
```

---

## 4. Ограничения

| Ограничение | Значение |
|---|---|
| Rate limit (заявленный) | Официальных цифр для MTProto нет; сервер отвечает `FLOOD_WAIT_X` — клиент обязан ждать X секунд. Для HTML-превью лимиты не документированы. |
| Rate limit (наблюдаемый) | При ~12 запросах к t.me/s/ подряд ничего не сработало. Долгая пагинация не проверена. |
| Лимит глубины выдачи | MTProto — вся история канала. HTML — не проверено. |
| Капча / антибот | В превью не встретилась. Для MTProto — SMS-код при логине; риск «подозрительной активности» на новом аккаунте реален. |
| Блокировка по IP замечена | Нет (за время проверки). Отдельный риск: аккаунт может быть ограничен/забанен за поведение бота — не проверено. |

---

## 5. Правовая проверка — обязательно

**Проверено 2026-09-16.** Запросы выполнены с сервера (IP Казахстана), поэтому
telegram.org отдал казахскую локаль независимо от пути `/tos/ru` и `/tos/en` и от
заголовка `Accept-Language`. Цитаты ниже — дословно как отданы, перевод мой и помечен.

| Вопрос | Ответ | Цитата / ссылка |
|---|---|---|
| `robots.txt` — что запрещает | **Файла нет.** `https://t.me/robots.txt` и `https://telegram.org/robots.txt` → 404 (nginx). Ни разрешения, ни запрета | проверено 2026-09-16 |
| ToS: запрещён ли автоматический сбор | **Запрета нет.** ToS целиком состоит из четырёх запретов (спам/обман, пропаганда насилия, незаконное порно, незаконная деятельность). Про автоматическое чтение, парсинг, скрапинг или роботов не сказано ничего | цитата ниже, https://telegram.org/tos |
| ToS: разрешено ли некоммерческое использование | Вопрос не регулируется: ToS не делит использование на коммерческое и некоммерческое | там же |
| ToS: разрешена ли публикация производных данных | Не регулируется ToS. Ограничение идёт не отсюда, а из авторского права — см. ниже | там же |
| Кого вообще связывает ToS | Зарегистрированных пользователей: «Telegram платформасында тіркелсеңіз» — «если вы регистрируетесь на платформе Telegram». Мы не регистрируемся и читаем анонимную веб-превью `t.me/s/<канал>` обычным HTTP | там же |
| Есть ли отдельные условия для API | Да — отдельные API Terms of Service для приложений на `api_id` | https://core.telegram.org/api/terms |

**Дословно, весь перечень запретов ToS** (казахская локаль, telegram.org/tos, 2026-09-16):

> Telegram платформасында тіркелсеңіз, Құпиялық саясатымызды қабылдайсыз және мыналарды істемеуге келісесіз:
> - Қызметімізді спам жіберу не қолданушыларды алдау үшін пайдалану.
> - Telegram-дағы жария арналарда, боттарда, тағы басқаларда зорлық-зомбылықты насихаттау.
> - Заңсыз порнографиялық контентті Telegram-дағы жария арналарда, боттарда, тағы басқаларда жариялау.
> - Көптеген елдерде заңсыз деп танылған әрекеттермен айналысу.

Перевод (мой): «Регистрируясь на платформе Telegram, вы принимаете нашу Политику
конфиденциальности и соглашаетесь не делать следующего: использовать наш сервис для
рассылки спама или обмана пользователей; пропагандировать насилие в публичных каналах,
ботах и прочем; публиковать незаконный порнографический контент; заниматься
деятельностью, признанной незаконной в большинстве стран».

Это весь документ по существу — дальше идут условия Telegram Premium.

**Вывод: блокер снят.** Запрета на автоматическое чтение публичных каналов в ToS нет,
robots.txt отсутствует. Мы не используем `api_id`, поэтому
[API Terms of Service](https://core.telegram.org/api/terms) к нам не применяются —
они связывают разработчиков приложений на MTProto. Читаем публичную веб-страницу
`t.me/s/<канал>` анонимным HTTP-запросом, как это делает любой поисковый робот.

**Что остаётся ограничением** — не ToS, а авторское право (ниже) и вежливость к сервису:
держим низкий темп запросов и честный User-Agent с контактной почтой.

**Авторские права на посты.** Текст вакансии — произведение автора поста (работодателя или админа канала). Хранение и тем более републикация полных текстов — это воспроизведение чужого текста. Наш подход по ТЗ снимает большую часть риска: **храним агрегаты (вилка, город, грейд, стек, дата) и ссылку на первоисточник вида `https://t.me/<channel>/<msg_id>`, полные тексты не воспроизводим публично.** Сырой текст, если он нужен для парсинга, держим только во внутреннем слое и не отдаём наружу.

---

## 6. Качество данных

Что есть в вакансии на этой площадке:

- [ ] Стабильный ID вакансии — **нет**; есть только ID сообщения
- [x] Дата публикации — время поста (но это дата поста, а не дата вакансии; репост той же вакансии даёт новую дату)
- [x] Название компании — почти всегда в тексте, отдельным полем нет
- [x] Зарплатная вилка — в тексте, но в профильных каналах почти всегда
- [x] Валюта — ₸ / сум / сом / $ вперемешку, в тексте
- [ ] Указание gross/net — редко и непоследовательно (у @headhunter_uz есть, т.к. это карточка hh)
- [x] Город — в тексте или хэштеге (#bishkek у @findwork)
- [x] Формат работы — обычно указан словом (офис/удалёнка/гибрид)
- [ ] Грейд явно указан — только в заголовке должности (Junior/Middle/Senior), отдельного поля нет
- [ ] Скиллы отдельным полем — нет, только внутри текста (хэштеги у @uzdev_jobs/@findwork — частично)
- [x] Полное описание — да, весь пост
- [ ] Признак «вакансия закрыта/архив» — **нет**. Пост живёт вечно; закрытие вакансии никак не отражается. Иногда правится текст (edit) — это ловится по `edit_date`.

**Есть ли стабильный `source_id`:** нет в смысле «ID вакансии». Технически стабильна пара `(channel_username, message_id)` → канонический URL `t.me/<channel>/<id>`. Её и надо брать как `source_id`, помня, что одна вакансия может иметь несколько таких ID.

**Пересечение с hh (на глаз):** низкое у профильных IT-каналов (@workitkz, @devkz_jobs, @uzdev_jobs, @findwork — ссылок на hh не замечено вообще, отклик идёт в личку рекрутеру в Telegram) и **полное у @headhunter_uz**, который прямо ретранслирует карточки hh.uz — его брать как источник смысла нет, он дублирует hh-коннектор. Отдельное пересечение: @findwork дублирует собственный сайт devkg.com — вакансии там же с ID `devkg.com/tg/j-<ID>`, и **разумнее парсить devkg.com, а не канал**, там ID стабильный.

---

## 7. Оценка трудозатрат

| Параметр | Оценка |
|---|---|
| Сложность подключения | средняя (забрать посты) / **высокая** (извлечь поля и дедуплицировать) |
| Время на реализацию | сбор через Telethon: 1–2 дня. Парсер полей + дедуп до приемлемого качества: 1–2 недели, и это работа на постоянное сопровождение. |
| Риск поломки при изменениях на стороне источника | высокий: у канала нет контракта. Админ поменял шаблон поста — парсер молча деградирует. HTML-превью может измениться в любой момент. |

---

## 8. ГЛАВНАЯ ПРОБЛЕМА: нет ID и нет структуры

Это ключевое отличие Telegram от hh, и оно определяет всё остальное.

**1. Нет ID вакансии.** Единственный стабильный ключ — `(channel, message_id)`, то есть идентификатор *сообщения*, а не *вакансии*. Следствия:
- одна вакансия, запощенная в трёх каналах, — это три разных `source_id`;
- репост той же вакансии через две недели (обычная практика — «поднять» объявление) — ещё один ID и новая дата публикации, из-за чего поток «новых вакансий в день» завышается;
- удалённое сообщение просто исчезает, и это не значит, что вакансию закрыли.

**2. Нет схемы.** Даже в самых аккуратных каналах шаблон — это соглашение админа, а не формат. Спектр по нашим каналам:
- **@findwork** — почти машиночитаемо: фиксированные поля, вилка, хэштег города, ссылка с ID. Парсится регулярками с хорошей точностью;
- **@workitkz, @devkz_jobs, @uzdev_jobs** — устойчивый, но «человеческий» шаблон: подписи полей плавают, эмодзи вместо ключей, порядок меняется. Регулярки дадут заметный процент промахов;
- **@vacancykgg** — свободный текст, регулярками не берётся.

**3. Что конкретно сложно извлечь:**
- **зарплата** — форматов много: «450 000–1 400 000 ₸», «до 1 000 000 ₸», «от $2000», «10–12 млн сум», «база 300К + KPI». Нужен нормализатор с валютой, периодом, признаком «до/от» и отсечением бонусной части, плюс курс на дату поста для сравнимости между ₸/сум/сом/$;
- **грейд** — только из заголовка, а заголовки пишут как попало;
- **стек** — только NER/словарь по тексту;
- **gross/net** — чаще всего просто отсутствует, и восстановить неоткуда.

**4. Дедупликация.** Точного ключа нет ни внутри Telegram, ни против hh. Реалистичный путь — только вероятностный: нормализованное `(компания + должность + город)` как блокирующий ключ, поверх него нечёткое сравнение текста (шинглы/MinHash или эмбеддинги) с порогом, и обязательное окно по времени. Осложняется тем, что компанию часто не пишут вовсе («крупный финтех», «продуктовая команда»), а отклик идёт в анонимную личку — то есть у части постов нет ни одного надёжного идентифицирующего поля.

**Практический вывод.** Telegram не даёт «строк в таблице» — он даёт текстовый поток, из которого поля надо извлекать вероятностно. Для метрик по зарплатам он ценен именно тем, что вилку указывают почти всегда. Правильная стратегия: начать с 3–4 самых структурированных каналов (**@findwork — а лучше сразу devkg.com, @workitkz, @devkz_jobs, @uzdev_jobs**), считать источник «вторым мнением» к hh, помечать все извлечённые поля флагом низкой достоверности и никогда не смешивать с hh в одной таблице без явного признака источника.

---

## 9. Что осталось непроверенным

- ~~ToS Telegram и API ToS — не прочитаны дословно~~ — закрыто 2026-09-16, см. §5.
- ~~`t.me/robots.txt` не запрашивался~~ — запрошен, 404.
- Telethon не запускался: `api_id`/`api_hash` не получены, поведение rate-limit'ов, `FLOOD_WAIT` и риски для аккаунта не измерены.
- @itmarket_uz — превью не отдало ленту постов; активность, формат и доля зарплат неизвестны.
- @headhunter_kz — публичного превью канала получить не удалось; существует ли аналог hh-ретранслятора для Казахстана, не установлено.
- Доли с зарплатой — глазомерные по ~20–30 постам, не по репрезентативной выборке.
- Глубина архива в HTML-превью (докуда работает `?before=`) не тестировалась.
- Реальный процент дублей между каналами и против hh не измерялся — оценка «низкое пересечение» сделана по отсутствию hh-ссылок, а не по сравнению текстов.
- Доля скама/рекламы/«вакансий» с бонусной схемой (особенно @jobkz_1, @vacancykgg) не оценивалась.


---

## Технический доступ — ПРОВЕРЕНО 2026-09-12, Telethon НЕ нужен

Главный вывод разведки пересмотрен: **api_id, api_hash и номер телефона
не требуются.** У публичных каналов есть веб-превью `t.me/s/<канал>`,
которое отдаётся обычным HTTP без авторизации.

```
GET https://t.me/s/workitkz            → 200, ~160 КБ HTML, 20 постов
GET https://t.me/s/workitkz?before=N   → следующие 20 постов
GET https://t.me/robots.txt            → 404 (robots.txt отсутствует)
```

Из каждого поста вытаскиваются:

| Поле | Источник в HTML |
|---|---|
| `post_id` | `data-post="channel/12345"` |
| `message_id` | числовая часть post_id |
| `published_at` | `<time datetime="2026-09-11T05:29:01+00:00">` — ISO с таймзоной |
| `views` | `tgme_widget_message_views` — «10.3K», парсится в 10300 |
| `text` | полный текст поста |
| `hashtags` | извлекаются из текста |

Реализация: `ingestion/sources/telegram.py`, запуск `python -m ingestion.run_telegram`.
Пауза между запросами 1 с — это веб-морда, а не API для машин.

### Просмотры — неожиданная находка

Счётчик просмотров есть у 100% постов. Это **не отклики**, но единственный
доступный сигнал интереса к вакансии во всём проекте: ни hh, ни другие
площадки ничего подобного не публикуют.

Что с ним можно делать: сравнивать интерес к направлениям, ловить всплески,
считать «просмотров на пост» по каналам. Чего нельзя: выдавать за отклики —
просмотр поста в ленте не означает даже прочтения.

### Форматы постов — у каждого канала свой

Проверено на живых данных 2026-09-12.

**@workitkz** — жёсткий шаблон, поля размечены явно:

```
#вакансия #астана #гибрид #workITkz #fullstack
Должность: Senior Fullstack разработчик
Компания: ТОО Digital Alliance
Город: Астана
Занятость: гибрид
Оплата: 800000
```

**@findwork (DevKG)** — свой шаблон, зарплата отдельной строкой с валютой:

```
EDU BRIDGE: Куратор по поступлению
Тип: Работа в офисе
30000 - 70000 KGS в месяц
Тэги: #edubridge #office #monthly #bishkek
Требования, контакты и условия тут: https://devkg.com/tg/j-22899
```

Важно: у findwork **есть внешний стабильный ID** — ссылка `devkg.com/tg/j-22899`.
Это единственный телеграм-канал, где вакансия имеет собственный идентификатор
вне телеграма, что сильно упрощает дедупликацию и отслеживание.

**@devkz_jobs** — полушаблон: хештеги с городом и стеком, дальше свободный текст.
Зарплата встречается не всегда.

**@uzdev_jobs** — аналогично, плюс более длинные тексты (до 2700 символов).

### Замер на 40 свежих постах каждого канала

| Канал | Постов | Просмотры | Формат |
|---|---|---|---|
| workitkz | 40 | 100% | жёсткий шаблон, зарплата почти всегда |
| findwork | 40 | 100% | шаблон + внешний ID на devkg.com |
| devkz_jobs | 40 | 100% | полушаблон, зарплата не всегда |
| uzdev_jobs | 40 | 100% | полушаблон, длинные тексты |

---

## Правовое — ЗАКРЫТО 2026-09-16

- `t.me/robots.txt` → **404**, файла нет: ни разрешения, ни запрета
- Условия использования Telegram **прочитаны и процитированы дословно** (§5).
  Запрета на автоматическое чтение публичных каналов в них нет
- ToS адресован тем, кто регистрируется на платформе; мы не регистрируемся
- API ToS не применяются: `api_id` не используем, MTProto не трогаем
- Остаётся авторское право на тексты постов — поэтому наружу отдаём агрегаты
  и ссылку на первоисточник, а не текст

**Подход по ТЗ §10:** храним агрегаты и ссылку `t.me/<channel>/<id>`,
полные тексты чужих объявлений не публикуем. Это снимает основной риск
по авторскому праву, но не заменяет чтение ToS.

**Прежде чем ставить сбор на расписание — прочитать Terms of Service Telegram
и записать сюда дословную цитату про автоматический доступ.**

---

## Главное ограничение остаётся

Стабильного ID вакансии нет — только `(channel, message_id)`. Следствия:

- репост той же вакансии = новая запись
- закрытие вакансии никак не отражается, пост просто висит
- времени жизни вакансии телеграм не даст — главную метрику проекта отсюда
  не посчитать
- дедупликация только вероятностная, а компанию указывают не всегда

Исключение — @findwork с его `devkg.com/tg/j-NNNNN`.
